import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    position: "absolute"

  },

  animat: {
    width: 380,
    height: 1010,
    resizeMode: "contain",

  },

});

export default styles;